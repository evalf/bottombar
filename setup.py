from setuptools import setup

setup(
  name = 'bottombar',
  version = '1.0b1',
  description = 'context manager that prints a status bar at the bottom of a terminal',
  author = 'Evalf',
  author_email = 'info@evalf.com',
  url = 'https://github.com/evalf/bottombar',
  py_modules = ['bottombar'],
  license = 'MIT',
  python_requires = '>=3.3',
)
